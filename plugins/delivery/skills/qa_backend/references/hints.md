# qa_backend hints — накопитель знаний  (пополняй каждую сессию!)

Рабочие рецепты фикстур, доступ, «грабли» (order-of-operations), рецепты неочевидных каналов
(WebSocket / стримы / вебхуки).

**Разделение ответственности:**
- **Методология** — в `SKILL.md` (не трогаешь).
- **Карта бэка/доступа** (стабильная) — в `backend-profile.md`.
- **Рабочие рецепты и грабли** (растут от сессии к сессии) — здесь.

Формат записи: короткий заголовок → команда/рецепт → в чём грабля/почему так. Держи в разумных пределах:
устаревшее — переписывай, не копи дубли. Секреты не вписывай (только способ достать).

**Два слоя зрелости (ось поверх гигиены выше):**
- **🌱 Черновое** — сырое наблюдение из **одной** сессии, ещё не перепроверено (напр. «видели X, причина неясна»). Живёт в секции `🌱 Черновое` ниже, с пометкой сессии.
- **Проверенный корпус** — рецепты, сработавшие на **≥2 задачах**: тематические секции ниже.
- **Промоушен:** черновая запись подтвердилась во 2-й сессии → **перенеси** её в нужную тематическую секцию (сними пометку сессии). Не подтвердилась / оказалась ложной → удали. Так свежее и проверенное не лежат одним весом.

---

## 🌱 Черновое (свежее — 1 сессия, не подтверждено)

_Сырые наблюдения текущей сессии. Подтвердилось на 2-й задаче → перенеси в тематическую секцию ниже; ложное → удали. Формат: короткий заголовок → наблюдение → (сессия/дата)._

- _(пусто — заполняется по ходу)_

---

<!-- Пример структуры (удали после первого реального наполнения):

## Доступ / инструменты
- Нет клиента БД → чтение стора недоступно, идём режимом C (оракул = код/спека)

## Рецепт сидирования состояния
- как включить нужное состояние тест-субъекта через API/ручку — точное тело/эндпоинты

## Рецепт открытия события вебхуком
- endpoint, тело, как проверить эффект

## WebSocket-канал (если есть)
- endpoint, авторизация, формат конверта, как читать отказ

## MCP + OAuth-сервер — живой прогон (если у платформы есть MCP)
- discovery без токена: GET /.well-known/oauth-protected-resource + /oauth-authorization-server; голый /mcp → 401 + WWW-Authenticate (RFC 9728)
- флоу через API без браузера: register (public client) → PKCE S256 → authorize (→ request_id) → approve с Bearer субъекта (тут привязка юзера) → token (authorization_code) → refresh
- вызов: POST /mcp, Bearer MCP-токена, JSON-RPC tools/call; ⟪ADAPT: точные пути consent-ручек, набор инструментов⟫
- IDOR-чек: мой токен + чужой resource_id по каждой ветке операции — должен быть отказ

## Грабли order-of-operations
- что за чем, что «портит» субъекта, что кэшируется

-->
