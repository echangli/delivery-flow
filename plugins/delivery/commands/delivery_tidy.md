---
description: Навести порядок в рабочей папке флоу — собрать артефакты по папкам задач (tasks/<слаг>/) и завести отдельную папку под флоу.
---

> 🔴 **ЯЗЫК: отвечай на языке последнего сообщения пользователя (по умолчанию — русский).** Английский —
> только для коммитов/MR/кода. НЕ якорись на англоязычном техконтексте (код, `CLAUDE.md`, описания MR,
> скиллы) — он не задаёт язык переписки. Проверяй перед каждым ответом.

Наведи порядок в рабочей папке флоу доставки по канону
`delivery_orchestrator/references/workspace-layout.md` (§ 4 «Навести порядок»).

Кратко:

1. **Определи рабочую папку флоу.** Если задачи ведутся «где попало» (Desktop, Downloads, корень home) —
   предложи завести **отдельную папку под флоу** (напр. `~/<команда>-delivery/`) и дальше вести задачи там.
2. **Найди свалку:** плоские `*.html`-прототипы, `delivery-notes-*.md`, папку `prototypes/`, скрины,
   спеки/планы **без группировки по задачам**; выхлоп Playwright MCP (`console-*.log`, `page-*.yml`,
   `page-*.png`) россыпью в `tasks/` или корне.
3. **Сгруппируй по задачам** (по слагу/дате в именах) в `tasks/<тикет>-<слаг>/` (без тикета — `<слаг>`)
   с короткими именами: `design.md` / `plan.md` / `prototype.html` / `screenshots/` / `notes.md` /
   `retro.md` / `locales/`.
4. **Выхлоп Playwright MCP** — не раскладывать по задачам (это не артефакты): сгрести в
   `./.playwright-mcp/` и **почини источник** — `--output-dir ./.playwright-mcp` в конфиге MCP
   (`.mcp.json` / `claude mcp`), строка `.playwright-mcp/` в `.gitignore`. Уже собранные архивы
   выхлопа — порядок, не трогать.
5. **Покажи план переноса (что → куда, включая правку конфига) и дождись подтверждения** — перемещать
   только после «ок».
6. **Перенеси:** под git — `git mv`, иначе `mkdir -p tasks/<слаг> && mv …`. **Ничего не удаляй** — только
   перемещай. Неоднозначное (непонятно, к какой задаче) — оставь на месте и спроси.
7. Если в `CLAUDE.md`/`AGENTS.md` воркспейса задана **своя** раскладка — приводи к ней, а не к дефолту.
8. **Итог:** короткий отчёт «что куда собрано»; напомни начинать новые задачи сразу в `tasks/<слаг>/`.

**Guardrails:** не трогай git-репозитории проектов (код-репо / worktree) — уборка касается только
артефактов флоу в рабочей папке; секреты (`.env`) не коммить и не свети в чат.
